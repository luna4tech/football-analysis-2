You are an expert computer vision and multi-object tracking engineer.

Refactor the existing repository into a modular, testable, reusable tracking pipeline that combines **DeepEIoU** and **GtaLink**.

Do not implement this as one long script. Each component must be independently runnable and verifiable.
The final implementation must support both:
- running the full end-to-end pipeline
- running each stage independently from cached intermediate files

Constraints:
- Preserve existing DeepEIoU and GtaLink behavior as much as possible.
- Prefer minimal, verifiable changes over a large rewrite.

Requirements:
1. DeepEIoU already computes ReID embeddings. GtaLink currently recalculates ReID embeddings. I want to avoid recomputing ReID embeddings inside GtaLink and instead pass/reuse the embeddings generated by DeepEIoU.
2. Each stage must be executable independently
3. Each stage must save its output to disk so that the future runs can reuse cached intermediate results instead of recomputing them.
4. Each stage must be verifiable with a small CLI command or test
5. I also want detailed profiling for every pipeline stage, including:
	  - execution time
	  - GPU memory usage
	  - CPU memory usage
	  - input/output shapes or counts where relevant
6. Create a user guide md document explaining 
	- how to run each stage and full pipeline
	- how to force recomputation
	- how to verify outputs
	- how to inspect profiling results.